---
const currentYear = new Date().getFullYear();
---

<footer>
	<div class="group">
		<p>MichaelDevOps — Backend & DevOps Engineer</p>
		<p>&copy; {currentYear} Michaeldevops</p>
	</div>
	<p class="socials">
		<a href="mailto:michealdevops@gmail.com">michealdevops@gmail.com</a>
		<a href="https://github.com/michaeldev0ps">GitHub</a>
	</p>
</footer>
<style>
	footer {
		display: flex;
		flex-direction: column;
		gap: 2rem;
		margin-top: auto;
		padding: 3rem 2rem 3.5rem;
		text-align: center;
		color: var(--gray-300);
		font-size: var(--text-sm);
		border-top: 1px solid rgba(148, 163, 184, 0.15);
	}

	footer a {
		color: var(--gray-200);
		text-decoration: none;
		transition: color var(--theme-transition);
	}

	footer a:hover,
	footer a:focus {
		color: var(--accent-light);
	}

	.group {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.socials {
		display: flex;
		justify-content: center;
		gap: 1rem;
		flex-wrap: wrap;
	}

	@media (min-width: 50em) {
		footer {
			flex-direction: row;
			justify-content: space-between;
			padding: 2.5rem 4.5rem 3rem;
		}

		.group {
			flex-direction: row;
			gap: 1rem;
			flex-wrap: wrap;
		}

		.socials {
			justify-content: flex-end;
		}
	}
</style>
